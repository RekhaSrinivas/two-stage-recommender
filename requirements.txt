# Phase 1 core (pinned for reproducibility).
# numpy is held at <2 on purpose: TensorFlow (Phase 2) and PyTorch Geometric
# (Phase 3) still expect the numpy 1.x ABI. Pinning it now avoids a painful
# reinstall later.
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
pyyaml==6.0.2
tqdm==4.66.5
threadpoolctl==3.5.0
pytest==8.3.3

# Phase 2: two-tower retrieval + ANN serving
# torch is installed from the CPU wheel index, not PyPI:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cpu
torch==2.5.1
faiss-cpu==1.14.3

# Phase 4: LambdaMART ranking stage
lightgbm==4.7.0
scikit-learn==1.7.2
